const btn = document.getElementById('confirm');
const input = document.querySelector('.task')
const list = document.querySelector('ol');

this.tasks = new List();

btn.addEventListener('click', () => {
    
    tasks.addTask(input, list);
})


